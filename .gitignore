# Virtual environment
.venv/

# Claude Code local tooling state
.claude/

# Python
__pycache__/
*.pyc

# Runtime data (uploaded reference photos/videos, rendered outputs) — never commit user media
data/uploads/*
data/outputs/*
data/thumbnails/*
!data/uploads/.gitkeep
!data/outputs/.gitkeep
!data/thumbnails/.gitkeep
